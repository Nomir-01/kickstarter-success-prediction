# Original notebooks

These notebooks are retained as historical coursework artifacts. They contain saved Colab state and overlapping experimental sections, so they are not the canonical reproducible implementation.

Use `src/train_models.py` for a clean run. It applies one shared train/test split, keeps preprocessing inside each model pipeline, imports every estimator explicitly, and learns Bayesian-network bins, structure, and parameters exclusively from training data.
